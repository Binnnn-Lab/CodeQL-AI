def lib_some_lib(arg: str) -> str:
    """hanlde a string input

    Args:
        arg (str): input arg, a str

    Returns:
        str: another str
    """
    return f"lib_some_lib({arg})"
