def some_tool(input: str="default"):
    """a tool for AI

    Args:
        input (str, optional): input string. Defaults to "default".

    Returns:
        _type_: another string.
    """
    return f"Input is {input}"
